#include <stdio.h>
/**
  * main - shows file's name
  * @argc: argument length
  * @argv: contains arguments
  * Return: 0
  */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
